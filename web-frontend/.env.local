# fixes VM watcher missing new files
VITE_WATCH_POLLING=true
VITE_WATCH_POLLING_INTERVAL=250

# optional (only if your API proxy target isn’t localhost from Vite’s POV)
# VITE_API_PROXY_TARGET=http://127.0.0.1:4000
# VITE_WS_PROXY_TARGET=ws://127.0.0.1:7777